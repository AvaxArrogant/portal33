declare module 'pdf-lib' {
  // Minimal type shim to satisfy TypeScript in this project context.
  // If you need full typings, install @types/pdf-lib (if available) or rely on the package's bundled types.
  export const PDFDocument: any;
}
